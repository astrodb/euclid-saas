# Ansible inventory for creating and managing infrastructure on Sausage.Cloud

# Ansible Shade uses OpenStack clients running locally
[cluster_sausage]
localhost ansible_connection=local ansible_python_interpreter=python

[openstack:children]
cluster_sausage
