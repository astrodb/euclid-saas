# Ansible inventory for creating and managing infrastructure on Cambridge Cumulus Cloud

# Ansible Shade uses OpenStack clients running locally
[cluster_cam]
localhost ansible_connection=local ansible_python_interpreter=python

[openstack:children]
cluster_cam
