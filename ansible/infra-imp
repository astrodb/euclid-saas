# Ansible inventory for creating and managing infrastructure at Imperial

# Ansible Shade uses OpenStack clients running locally
[cluster_imp]
localhost ansible_connection=local ansible_python_interpreter=python

[openstack:children]
cluster_imp
